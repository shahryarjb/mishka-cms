defmodule MishkaHtmlWeb.PageLive do
  use MishkaHtmlWeb, :live_view

  @impl true
  def mount(_params, _session, _socket) do
  end

  @impl true
  def handle_event("suggest", _params, _socket) do

  end

  @impl true
  def handle_event("search", _params, _socket) do
  end
end
